# frozen_string_literal: true

source "https://rubygems.org"

# Dit is exact de gem-bundel die GitHub Pages zelf gebruikt om te bouwen,
# zodat lokaal testen (`bundle exec jekyll serve`) hetzelfde resultaat
# geeft als de live site.
gem "github-pages", group: :jekyll_plugins
